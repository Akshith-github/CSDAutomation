# Stepwise project setup

1) Install Box , upload excel files given in "Files to be in box" folder and setup couchdb database

2) pip install -r requirements.txt

3) update .env file with paths and urls

3) python run.py   # to run
   python test_csd.py  # to tset